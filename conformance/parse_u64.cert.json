{
  "item": "parse_valid",
  "level": "L3",
  "contract_quality": {
    "tautology": false,
    "vacuous_precondition": false
  },
  "effects": ["pure"],
  "slag": false,
  "note": "Stable-subset oracle (goal.md R-CHAR-3): item/level/tautology/vacuous_precondition/effects/slag are hand-derived from .design/basis/09-option-result.md AC-4 + 07-strings.md REQ-9 (parse_u64 with the strengthened usable contract) + thermite-design.md §6, and are oracle-compared. mutants_killed + solver_time_ms EXCLUDED (oracle_subset, §5.3). `parse_valid` proves L3: given `req all_digits(s) && s.len() >= 1 && parse_be(s) <= u64::MAX`, parse_u64's strengthened contract discharges `ens result is Some` AND the round-trip `Some(v) => parse_be(s) == v`. The contract is non-vacuous: an always-None mutant violates `result is Some` (the req makes None impossible for valid in-range input), so the mutation gate kills it -> L3. fx pure. The sibling `parse_rejects_nondigit` pins the handled-or-loud teeth (a non-digit string is REFUSED to None, never a wrong value) and certifies L3 likewise. This is the calculator's parse primitive: a valid digit string in range -> Some(value), proven for all such inputs; malformed -> a loud None."
}
