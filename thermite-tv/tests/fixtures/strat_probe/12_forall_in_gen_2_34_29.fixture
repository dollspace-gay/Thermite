# strat_probe fixture — gen#2.34#29
shape: forall_in
source: gen#2.34#29
origin: generated
hit: true
--- production ---
forall i . 0 <= i < len(ys) => idx(ys, i) != 50
--- reference ---
forall w . (w < len(ys) & 0 <= w) => idx(ys, w) != 50
