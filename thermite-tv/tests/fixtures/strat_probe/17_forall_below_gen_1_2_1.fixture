# strat_probe fixture — gen#1.2#1
shape: forall_below
source: gen#1.2#1
origin: generated
hit: true
--- production ---
forall i . 0 <= i < k & i < len(xs) => idx(xs, i) as u32 != 53
--- reference ---
forall w . (w < len(xs) & w < k & 0 <= w) => idx(xs, w) as u32 != 53
