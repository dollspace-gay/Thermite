enum Shape {
  Circle(u64),
  Rect { w: u64, h: u64 },
}

fn is_circle(s: Shape) -> bool
  req true
  ens result == (s is Circle)
  fx  pure
{
  match s {
    Circle(r)     => true,
    Rect { w, h } => false,
  }
}
