[package]
name = "thermite-kernel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
thermite-verified = { path = "../thermite-verified" }
