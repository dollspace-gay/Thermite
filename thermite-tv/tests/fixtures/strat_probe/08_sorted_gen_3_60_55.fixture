# strat_probe fixture — gen#3.60#55
shape: sorted
source: gen#3.60#55
origin: generated
hit: true
--- production ---
forall i j . 0 <= i <= j < len(ys) => idx(ys, i) <= idx(ys, j)
--- reference ---
forall b a . (b < len(ys) & a <= b & 0 <= a) => idx(ys, b) >= idx(ys, a)
