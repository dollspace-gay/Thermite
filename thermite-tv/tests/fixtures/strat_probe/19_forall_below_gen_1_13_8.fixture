# strat_probe fixture — gen#1.13#8
shape: forall_below
source: gen#1.13#8
origin: generated
hit: true
--- production ---
forall i . 0 <= i < k & i < len(xs) => idx(xs, i) >= 52
--- reference ---
forall w . (w < len(xs) & w < k & 0 <= w) => idx(xs, w) >= 52
