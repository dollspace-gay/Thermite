[package]
name = "thermite-spec"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

# REQ-2: depends on thermite-syntax (consumes the AST).
[dependencies]
thermite-syntax = { path = "../thermite-syntax" }
