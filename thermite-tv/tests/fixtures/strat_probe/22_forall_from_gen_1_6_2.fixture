# strat_probe fixture — gen#1.6#2
shape: forall_from
source: gen#1.6#2
origin: generated
hit: true
--- production ---
forall i . 14 <= i < len(ys) => idx(ys, i) >= 62
--- reference ---
forall w . (w < len(ys) & 14 <= w) => idx(ys, w) >= 62
